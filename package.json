{
  "name": "gym-master-spa",
  "version": "1.0.0",
  "description": "Premium Gym Management Single Page Application",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cd dist && git init && git checkout -B gh-pages && git add . && git commit -m \"deploy: update github pages\" && git push -f https://github.com/siwakornsomrak01-pixel/gym-master-frenchgym.git gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
